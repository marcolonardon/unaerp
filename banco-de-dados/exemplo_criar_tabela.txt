CREATE TABLE CLIENTE(
ID_CLIENTE INTEGER,
NOM_CLIENTE VARCHAR2(40) NOT NULL,
CPF_CLIENTE CHAR(11) UNIQUE,
DTN_CLIENTE DATE,
CONSTRAINT PK_CLIENTE PRIMARY KEY(ID_CLIENTE)
);

CREATE TABLE TELEFONE(
ID_CLIENTE INTEGER,
DDD_TELEFONE NUMBER(2),
NUM_TELEFONE VARCHAR2(10),
CONSTRAINT PK_TELEFONE PRIMARY KEY(ID_CLIENTE, DDD_TELEFONE, NUM_TELEFONE),
CONSTRAINT FK_TELEFONE_CLIENTE FOREIGN KEY(ID_CLIENTE) REFERENCES CLIENTE
);

-- visualizar a estrutura da tabela
	DESCRIBE NOME_TABELA ou DESC NOME_TABELA;

-- visualizar as tabelas do tablespace
	SELECT * FROM NOME_TABELA;

-- visualizar o nome das tabelas do usuário conectado
	SELECT TABLE_NAME FROM USER_TABLES;

-- visualizar o nome das constraints do usuário conectado
	SELECT CONSTRAINT_NOME FROM USER_CONSTRAINTS;

-- alterar tabela
	ALTER TABLE
-> ADD
-> MODIFY
-> DROP [COLUMN | CONSTRAINT]

-- exemplo de alteração
	ALTER TABLE CLIENTE
	ADD SEX_CLIENTE CHAR(1);

	ALTER TABLE CLIENTE
	MODIFY NOM_CLIENTE VARCHAR2(50);

