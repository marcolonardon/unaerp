CREATE TABLE "NOME_TABELA"(
atributo1 tipo1 (tamanho) [UNIQUE | NOT NULL],
atributo2 tipo2 (tamanho),
atributo3 tipo3 (tamanho),
CONSTRAINT "nome" [PRIMARY KEY | FOREIGN KEY | CHECK] parâmetros
)

	CREATE TABLE "PRODUTO" (
        COD_PRODUTO INTEGER,
        NOM_PRODUTO VARCHAR2(150),
        DSC_PRODUTO VARCHAR2(150),
	CONSTRAINT PK_PRODUTO PRIMARY KEY(COD_PRODUTO)
        );
  


	CREATE TABLE "FORNECEDOR" (
        COD_FORNECEDOR INTEGER,
	RAZ_SOCIAL_FORNECEDOR INTEGER,
	CNPJ_FORNECEDOR INTEGER,
	IE_FORNECEDOR INTEGER,
        EMAIL_FORNECEDOR VARCHAR2(50),
	CONSTRAINT PK_FORNECEDOR PRIMARY KEY(COD_FORNECEDOR)
        );

	ALTER TABLE FORNECEDOR
	MODIFY RAZ_SOCIAL_FORNECEDOR VARCHAR2(50);

	ALTER TABLE FORNECEDOR
	MODIFY CNPJ_FORNECEDOR VARCHAR2(50);

	ALTER TABLE FORNECEDOR
	MODIFY IE_FORNECEDOR VARCHAR2(50);

